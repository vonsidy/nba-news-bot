# secrets & runtime state
.env
state.json
sample_card.png

# runtime-fetched assets (font + any logos are pulled at run time, never committed)
assets/

# python
.venv/
__pycache__/
*.pyc

# node / next
node_modules/
.next/
.vercel
